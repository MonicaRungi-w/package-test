import React from "react";
import { render } from "@testing-library/react";

import Tab from "./Tab";

describe("Tab", () => {
  test("renders the Tab component", () => {
    render(<Tab />);
  });
});
